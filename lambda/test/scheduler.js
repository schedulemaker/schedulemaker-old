'use strict';

var assert = require('assert');
var Scheduler = require('scheduler');

describe('#Scheduler', function(){
    var scheduler = new Scheduler();

    it('Should not allow time conflicts', function(){
        
    });
});